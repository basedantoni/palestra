{
  "name": "@src/api",
  "type": "module",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    },
    "./*": {
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui"
  },
  "dependencies": {
    "@src/auth": "workspace:*",
    "@src/db": "workspace:*",
    "@src/env": "workspace:*",
    "@src/shared": "workspace:*",
    "@trpc/client": "catalog:",
    "@trpc/server": "catalog:",
    "date-fns": "^4.1.0",
    "dotenv": "catalog:",
    "drizzle-orm": "^0.45.1",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@src/config": "workspace:*",
    "@vitest/ui": "^4.0.18",
    "hono": "catalog:",
    "typescript": "catalog:",
    "vitest": "^4.0.18"
  }
}
